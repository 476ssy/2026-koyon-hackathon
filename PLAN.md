# 2026 연고전 해커톤 — 25일 준비 계획

대회 9/27~10/1 개발 · 10/6 파이널데이 / 목표 상위 3 / 가용 3~4h·일 / 3인 팀
작성 9/2. 공식 룰·베이스라인 공개 시 즉시 갱신.

## 전제: 대회 중 팀 전원이 LLM을 적극 사용한다

그래서 병목이 **타이핑에서 판단으로** 옮겨간다. 코드는 빨리 나오지만
"이 변경이 실제로 더 강한가"를 가려내는 비용이 남는다. 계획은 이 전제 위에 선다.

- 최대 실패 모드 — LLM이 그럴듯한 전략 코드를 뽑았는데 실제로는 더 약하고,
  검증 없이 채택돼 순위가 내려간다. **자동 승률 게이트가 없으면 LLM은 순위를 떨어뜨린다.**
- 그래서 검증 인프라(러너·게이트·로그)를 알고리즘보다 먼저 세운다.
- LLM에 컨텍스트를 못 주면 LLM은 장님이다. 룰 문서와 리플레이 요약 도구가 필요하다.

### 무엇을 직접 하고 무엇을 위임하나

| | 항목 | 이유 |
|---|---|---|
| 직접 (1회씩) | 알파베타, BFS 거리맵, 평가함수, 승률 통계 | 개념 체화. 대회 중 즉시 고치려면 이해가 필요 |
| 위임 | 보일러플레이트, 리팩토링, 시각화, 로그 파싱, 문서 | 이해도가 결과를 안 바꿈 |
| 절대 위임 금지 | **채택 여부 판단** | 게이트를 통과 못 하면 무조건 버린다. 예외 없음 |

## 연습 환경

공식 시뮬레이터 미공개 → 유사 대회 환경으로 대체. 둘 다 `kaggle_environments` 위에 있어
**러너·게이트·튜닝 코드를 그대로 재사용**한다. 환경 교체 자체가 룰 공개 시 이식 리허설이다.

| | planet_wars | reinforce_tactics |
|---|---|---|
| 성격 | 거점 점령 + 자원 배분 | 격자 + 지형 + 유닛 전투 |
| 판당 | 0.2초 | 0.45초 |
| 무승부 | 0/60 (실측) | 큰 맵 10/10 |
| 종료 | 200턴 후 함선 수 (점수형) | HQ 점령 / 전멸 |
| 내장 봇 | do_nothing, random, nearest_enemy | noop, random, aggressive, simple_bot |

실측 기준선 — `nearest_enemy vs random` W11/L9 · `simple_bot vs random` W9/L0/D1 (beginner)

### 세팅
```
python -m venv .venv
.venv/Scripts/python.exe -m pip install --no-deps kaggle-environments==1.32.7
.venv/Scripts/python.exe -m pip install -r requirements.txt
```

## Stage A — planet_wars: 기본기 + 검증 인프라 (D1~D8, 9/2~9/9)

룰이 단순해 알고리즘에만 집중할 수 있다. 무승부가 없어 승률 신호가 항상 잡힌다.

| D | 주제 | 검증 |
|---|---|---|
| 1 | obs(`planets`/`fleets`) · 액션 포맷 파악, 첫 봇 **직접 작성** | `do_nothing` 상대 20판 전승 |
| 2 | **대전 러너 v1** — N판 승률 + Wilson 신뢰구간, seed 고정, 실험 로그 JSONL | `random` 상대 승률 ≥ 70% |
| 3 | **승률 게이트** — `arena.py --gate <챔피언>`, 100판 신뢰구간 하한 < 50%면 기각 | 일부러 약화시킨 봇이 기각되는지 확인 |
| 4 | 평가함수 feature 설계 (함선 수, 성장률 합, 행성 수, 적 대비 차) | feature별 승률 기여도 측정 |
| 5 | 목표 선정 휴리스틱 — ROI = 성장률 / (필요 병력 × 거리) | `nearest_enemy` 상대 ≥ 70% |
| 6 | 알파베타 **직접 구현** — 후보 수 pruning 포함 | D5 대비 ≥ 60% |
| 7 | 상대 모델링 — 적 함대 도착 예측, 방어 필요 병력 계산 | 역전패율 감소 |
| 8 | 파라미터 튜닝(랜덤서치) + ELO 리그 | 버전별 ELO 표 산출 |

## Stage B — LLM 협업 체계 (D9~D12, 9/10~9/13)

대회 중 실제로 돌릴 루프를 미리 만들고 예행연습한다. 여기가 이번 개정의 핵심이다.

| D | 주제 | 검증 |
|---|---|---|
| 9 | **봇 레포용 `AGENTS.md`** — 룰 요약, 액션 스키마, 코드 구조, 검증 명령, 금지사항 | 이 문서만 주고 LLM에게 봇 개선 시켜 게이트 통과 |
| 10 | **리플레이 → 텍스트 요약 도구** — 패배 판을 압축 텍스트로 | 요약만 보고 패인을 지목할 수 있는지 |
| 11 | **코드 구조 리팩토링** — eval / search / policy 분리, 파라미터 단일 dict, 순수 함수 | LLM이 한 파일만 고쳐 개선 가능한지 |
| 12 | **LLM 개선 루프 리허설** — 리플레이 요약 → 가설 → 구현 → 게이트 → 채택/기각, 3사이클 | 3사이클 후 순 ELO 상승 |

## Stage C — reinforce_tactics: 격자 (D13~D19, 9/14~9/20)

격자·경로계획·지형. Stage A/B 인프라를 이식해 공용화한다.

| D | 주제 | 검증 |
|---|---|---|
| 13 | 격자 obs 파싱 (지형 / 유닛 / 건물 / gold) | `random` 상대 ≥ 80% (beginner) |
| 14 | 다중소스 BFS 거리맵 + 지형 비용 **직접 구현** | 거리맵 단위 테스트 |
| 15 | `seize` 우선순위 = 거점 가치 / 거리 | `simple_bot` 상대 ≥ 50% |
| 16 | 전투 계산 (ATK/DEF/HP, HP 스케일 데미지) → 교전 유불리 판단 | 교전 손익 예측 오차 측정 |
| 17 | **러너·게이트·로그를 두 환경 공용으로 이식** | 두 환경이 같은 CLI로 실행. **소요 시간 기록** |
| 18 | 큰 맵 무승부 → 자체 스코어 지표 설계 (점령 수 + gold + 유닛 가치) | crossroads에서 순위 신호 확보 |
| 19 | 새 환경에서 LLM 루프 재가동 (D12 반복) | 이식이 실제로 작동하는지 확인 |

D17의 소요 시간이 곧 "공식 룰 공개 후 이식에 며칠 걸리는가"의 실측치다.

## Stage D — 대회 대비 (D20~D25, 9/21~9/26)

| D | 주제 | 검증 |
|---|---|---|
| 20 | 프로파일링 · 턴 시간 예산 | 턴당 시간 측정, 상한 확보 |
| 21 | 로버스트니스 — 예외 · 타임아웃 · 무효 액션 방어 | 결함 주입 테스트 통과 |
| 22 | **3인 병렬 규율** — 브랜치 규칙, 봇 버전 네이밍, 리그 자동 등록, 머지 전 게이트 필수 | 3버전 동시 리그가 돌아감 |
| 23 | **팀 리허설** — 3인이 각자 LLM으로 봇 개선 → 리그 → 최강 선발 | 대회 하루치 전 과정 예행연습 |
| 24 | 보고서 · 발표 골격 + 실험 로그 → 표/그래프 자동 생성 | 로그에서 결과물이 자동으로 나옴 |
| 25 | 예비 — 공식 룰 · 베이스라인 공개 대응 | 어댑터 교체로 러너 재가동 |

## 대회 5일 운영 루프 (9/27~10/1)

중간 집계가 매일 오전 10시 · 오후 10시 → 하루 2사이클. 사이클마다:

```
집계 확인 → 패배 판 리플레이 요약 → 가설 1개 확정
  → LLM으로 구현 (컨텍스트: AGENTS.md + 리플레이 요약 + 해당 파일)
  → 게이트 100판. 신뢰구간 하한 < 50%면 기각, 예외 없음
  → 통과분만 머지 → 3인 버전 리그 → 최강 버전 제출
  → 실험 로그 1줄 기록 (보고서 재료)
```

가설은 사이클당 1개. 여러 개를 한 번에 바꾸면 뭐가 효과였는지 못 가린다.

## 역할 (3인)

| | 담당 | 근거 |
|---|---|---|
| 나 | 전략·알고리즘 코어 (평가함수 + 탐색), 최종 통합·제출 | 대회 중 코어를 직접 즉시 고쳐야 함 |
| B | 인프라 — 러너, 게이트, 튜닝, 로그/시각화, 프로파일링 | 병목은 가설 검증 속도 |
| C | 분석 — 리플레이 분석, 전략 가설 제안, 보고서·발표 초안 | 보고서 20% + 발표 10% |

전원 LLM을 쓰되 머지 권한은 한 명(나)에게 모은다. 게이트를 통과한 변경만 들어온다.

## 가정과 리스크

- **언어 Python 가정** — 베이스라인 제공 대회 대부분. 아니어도 Stage A~C 알고리즘은 이식된다.
- **유닛 수 불명** — 공식 설명에 유닛 언급이 없다. 단일이면 분기가 작아 탐색을 깊게 갈 수 있다.
  다중이면 유닛-거점 할당이 추가로 필요하다. planet_wars(유닛 없음) + reinforce_tactics(다중)로
  양쪽을 나눠 대비한다.
- **승리 조건 차이** — 우리 대회는 점수 누적형으로 보인다. planet_wars가 같은 구조,
  reinforce_tactics는 HQ 점령형이라 이 부분은 전이되지 않는다.
- **LLM 사용 규정 미확인** — 외부 라이브러리 허용 범위, 코드 출처 표기 의무, 턴 시간 제한을
  요강 공개 시 최우선 확인. 규정 위반은 순위가 아니라 실격이다.
- **가장 안 변하는 자산** — 러너 / 게이트 / 승률 통계 / 로그. 룰과 무관하게 100% 재사용된다.
